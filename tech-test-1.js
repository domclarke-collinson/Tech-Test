// Sort function

function sortArray(a, b) {
   return a - b
};

// define the arrays

const arrayP = [6, 2, 7];
const arrayS = [4, 6, 8];


// calculate the number of people total and number of spaces in the cars

let additionP = 0;
for (let a = 0; a < arrayP.length; a++) {
    additionP = additionP + arrayP[a];
}

let additionS = 0;
for (let a = 0; a < arrayS.length; a++) {
    additionS = additionS + arrayS[a];
}


console.log (additionP);
console.log (additionS);

// list the cars



